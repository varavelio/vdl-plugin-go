@meta({ family "string" })
enum DeliveryStateBase {
  @label("empty")
  Unknown = ""
  Pending
  InTransit = "in-transit"
}

@tag
enum DeliveryState {
  ...DeliveryStateBase
  @aliases(["done" "completed"])
  Delivered = "delivered"
  @deprecated("Use Delivered instead.")
  Returned = "returned"
}
